import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tripledot.blockbrainbuster',
  appName: 'Blocks Brain Buster',
  webDir: 'out'
};

export default config;
